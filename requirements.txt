# --- Dépôts personnalisés (VITAL POUR WINDOWS GPU) ---
--extra-index-url https://download.pytorch.org/whl/cu121

# --- PyTorch (CUDA 12.1) ---
torch==2.5.1+cu121
torchvision==0.20.1+cu121
torchaudio==2.5.1+cu121

# --- GPU / IA ---
insightface==0.7.3
onnxruntime-gpu==1.24.4
numpy==1.26.4
opencv-python==4.11.0.86      # dernière version compatible numpy 1.x (mediapipe impose numpy<2)
scikit-learn==1.8.0
scipy==1.17.1
setuptools<82                # deep-sort-realtime 1.3.2 importe pkg_resources, supprimé dans setuptools 82
ultralytics==8.4.39          # YOLOv8 — détection corps (pipeline Body-First)

# --- Tracking ---
deep-sort-realtime==1.3.2    # DeepSORT + ReID apparence (pipeline Body-First)
# deepsort-rs                # OPTIONNEL — association en Rust (TRACKER_BACKEND=rust).
                             # Pas sur PyPI : à construire localement, cf. README.
                             # deep-sort-realtime reste requis dans tous les cas,
                             # le backend Rust lui emprunte son embedder MobileNetV2.
mediapipe==0.10.21

# --- Web ---
flask==3.1.3
python-dotenv==1.2.2

# --- Utilitaires ---
colorama==0.4.6

# --- Dev / Qualité ---
ruff
mypy
pytest
